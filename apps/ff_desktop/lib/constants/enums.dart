enum SideBarSections {
  home,
  pinned,
  cloud,
  yours,
  drives,
  tags,
}

enum PredefinedFolders {
  home,
  desktop,
  downloads,
  documents,
  pictures,
  videos,
  movies,
  music,
}

enum ViewMode {
  list,
  details,
  grid,
  columns,
}
